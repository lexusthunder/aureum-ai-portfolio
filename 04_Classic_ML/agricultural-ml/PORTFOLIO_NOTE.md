# Portfolio Note — Agricultural ML

## What it is

Crop / yield prediction model in `main.py` plus exploratory notebook. Companion to the AgroBot in `01_LLM_Agents/multimodal-agent-agrobot`.

## Why it's relevant

Google's Agriculture vertical (e.g. Mineral, Wing) hires ML engineers who can model the messy real world. This project demonstrates that I model agricultural data, not just MNIST.

## Status

🟡 Working baseline. Roadmap: replace baseline with XGBoost + SHAP for explainability, deploy as a Flask endpoint behind the same MLflow tracking as `02_MLOps`.
